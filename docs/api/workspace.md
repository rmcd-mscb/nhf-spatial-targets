# Workspace

`nhf_spatial_targets.workspace` defines the `Project` dataclass that every CLI command and every fetch / aggregate / target builder threads through. It encapsulates the project directory, datastore path, fabric metadata, and the canonical path-resolution helpers.

::: nhf_spatial_targets.workspace
    options:
      show_source: true
      heading_level: 2
