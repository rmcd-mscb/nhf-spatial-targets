[project]
name = "nhf-spatial-targets"
version = "0.1.0"
description = "Curated calibration target datasets for the National Hydrologic Model"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64", "win-64"]

[dependencies]
python = ">=3.11"
pyyaml = ">=6.0"
xarray = ">=2024.1"
numpy = ">=1.26"
pandas = ">=2.0"
geopandas = ">=0.14"
rioxarray = ">=0.15"
netcdf4 = ">=1.6"
h5netcdf = ">=1.3"
dask = ">=2024.1"
click = ">=8.1"
rich = ">=13.0"
# gdptools: spatial aggregation (install from PyPI if not on conda-forge)
# sciencebasepy: ScienceBase access
# earthaccess: NASA EDL access for MODIS, MERRA-2, NLDAS

[pypi-dependencies]
nhf-spatial-targets = { path = ".", editable = true }
gdptools = "*"
sciencebasepy = "*"
earthaccess = "*"

[feature.dev.dependencies]
pytest = ">=8.0"
pytest-cov = ">=5.0"
ruff = ">=0.4"
mypy = ">=1.10"
ipykernel = ">=6.0"

[environments]
default = { solve-group = "default" }
dev = { features = ["dev"], solve-group = "default" }

[tasks]
# Run the full pipeline
run = { cmd = "nhf-targets run --config config/pipeline.yml", description = "Run the full target pipeline" }

# Run individual targets
run-runoff = { cmd = "nhf-targets run --config config/pipeline.yml --target runoff" }
run-aet    = { cmd = "nhf-targets run --config config/pipeline.yml --target aet" }
run-rch    = { cmd = "nhf-targets run --config config/pipeline.yml --target recharge" }
run-som    = { cmd = "nhf-targets run --config config/pipeline.yml --target soil_moisture" }
run-sca    = { cmd = "nhf-targets run --config config/pipeline.yml --target snow_covered_area" }

# Catalog inspection
catalog-sources   = { cmd = "nhf-targets catalog sources" }
catalog-variables = { cmd = "nhf-targets catalog variables" }

# Development
test  = { cmd = "pytest tests/ -v", description = "Run test suite" }
lint  = { cmd = "ruff check src/ tests/", description = "Lint source" }
fmt   = { cmd = "ruff format src/ tests/", description = "Format source" }
